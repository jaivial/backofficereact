import React from "react";import{beforeEach,describe,expect,it,vi}from"vitest";import{render,screen}from"@testing-library/react";import{CardReconciliation}from"./CardReconciliation";
describe("CardReconciliation",()=>{beforeEach(()=>vi.stubGlobal("fetch",vi.fn(async()=>new Response(JSON.stringify({success:true,items:[{date:"2026-07-28",payments:3,amountCents:7250,referencedPayments:3,uniqueReferences:3,referencesComplete:true,referencesUnique:true}]})))));it("shows standalone terminal totals",async()=>{render(<CardReconciliation/>);expect(await screen.findByText(/72,50/)).toBeInTheDocument();expect(screen.getByText("3 referencias únicas")).toBeInTheDocument()})});
